// Models Index — exports all models and sets up associations
// TODO: Implement in Lab 4

// const sequelize = require('../db/config');
// const User = require('./User');
// const Movie = require('./Movie');
// const UserMovie = require('./UserMovie');

// // Associations
// User.belongsToMany(Movie, { through: UserMovie, foreignKey: 'userId' });
// Movie.belongsToMany(User, { through: UserMovie, foreignKey: 'movieId' });
// User.hasMany(UserMovie, { foreignKey: 'userId' });
// UserMovie.belongsTo(User, { foreignKey: 'userId' });
// UserMovie.belongsTo(Movie, { foreignKey: 'movieId' });

// module.exports = { sequelize, User, Movie, UserMovie };
